{# wrapper Live #}
<div {{ attributes }} class="mptp-user-view container-fluid">
    {% include '@MultiProjectTeamPlanning/components/user/_toolbar.html.twig' with {
        ui: this.ui,
        comp: this,
    } only %}

    {% include '@MultiProjectTeamPlanning/components/user/_grid.html.twig' with {
        ui: this.ui,
        comp: this,
        days: this.daysForView,
        grid: this.grid
    } only %}
</div>

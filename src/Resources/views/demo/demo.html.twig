{# mode: 'user' | 'project' #}
{# userId: id | Uuid #}
{#period: 'day' | 'week' | 'workweek' | 'month' #}
{#anchor: 'YYYY-MM-DD' | 'today'#}

{% extends "base.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('bundles/multiprojectteamplanning/css/planner.css') }}">
{% endblock %}
{% block body %}
    {{ component('planner_user_view', {
        mode: 'user',
        userId: 1,
        period: 'week',
        anchor: 'today'
    }) }}
{% endblock %}